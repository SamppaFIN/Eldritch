# Handover — 2026-08-31 ilta, konevaihto

Tämä on **kertaluontoinen tilannekuva**, ei elävä tila-asiakirja. Se on kirjoitettu
siirtymistä varten toiselle koneelle, ja se vanhenee heti seuraavasta committista.
Totuuden lähde on aina `docs/tickets/` — tämä tiedosto viittaa sinne, ei toista sitä.
Voi poistaa kun on luettu. Sama malli kuin `PIVOT-2026-08-27.md`:llä: päivätty
kertaluontoinen merkintä, ei tikettien korvike.

## Mitä juuri tehtiin (5 committia, kaikki pushattu `docs/vaihe-3-sivilisaatio-tiketit`)

```
aaffff6 Nine resources, a storage cap, dormancy — and the pool's own migration bug
e7f7ba8 The Keep is on the map now, and it plugs a real leak in the Wager
3563d10 The Keep: a public decoy near the Hearth, data layer done
27805e6 Batch the reads into one transaction, and retract a wrong claim
c817b0a Split the cell store out, and IndexedDB stops reading everything
```

Jokainen committi ajettiin läpi `pnpm test && pnpm typecheck && pnpm lint:lines`
ennen pushia. Tila juuri nyt: **434 testiä, kaikki vihreitä**, `tsc -b` puhdas,
`lint:lines` puhdas (mikään tiedosto ei ylitä 400 riviä).

### BRDC-CASTLE-001 — 90 %, `in_progress`
Linna (julkinen kotipesän korvike) on toteutettu kokonaan paitsi `world.json`-vientiä,
joka ei ole vielä mahdollista koska `world.json`ia ei ole olemassa (`BRDC-SHARE-001`).
Sivulöytö: Wager (`exportChallenge`) vuosi oikean kotipesän jokaiselle haastetulle
kaverille ennen tätä — korjattu.

### BRDC-ECON-001 — 95 %, `done`
Yhdeksän resurssia (ei kymmenen — oma otsikkoni oli väärässä, korjattu), tuotantokatto,
lepotila. Sivulöytö: `SAVE_VERSION` ei koskaan suojannut resurssipoolia (se suojaa vain
`localStorage`a; pooli asuu IndexedDB:ssä ilman versiota) — korjattu suoraan
`pouch.ts`ssä, yleisempi puute avattu omaksi tiketiksi `BRDC-PERSIST-002`.

### BRDC-SCALE-001 — 40 %, `in_progress`
`MockRepository`-jako ja IndexedDB-transaktioiden niputus (`getMany`) tehty. Alkuperäinen
suunnitelma alueperusteiselle kyselylle (H3-merkkijonon etuliite) **oli väärä ja
kumottiin todisteineen** — oikea ratkaisu (yhdistelmäavain) odottaa `BRDC-PERSIST-002`:ta.

### BRDC-PERSIST-002 — uusi, `todo`
Ei aloitettu. Pieni (M), rajattu, ja avaa `BRDC-SCALE-001`:n viimeisen kohdan.

## Suositeltu seuraava askel

**`BRDC-PERSIST-002`** ensin — se on pieni, itsenäinen, ja ratkaisee kaksi asiaa
kerralla: oman itsensä ja `BRDC-SCALE-001`:n jäljellä olevan kohdan (avainmuodon
vaihto `cell:${h3}` → `cell:${regionOf(h3)}:${h3}`).

Sen jälkeen luonnollinen jatkumo on jompikumpi:
- **`BRDC-SHARE-001`** (world.json + cron) — avaa `BRDC-CASTLE-001`:n viimeisen kohdan
- **`BRDC-TERRAIN-002`** (maastokirjo 4 → 9) — `BRDC-ECON-001` on nyt tehty, joten
  rakennusketju (`BRDC-BUILD-001` jne.) on periaatteessa auki, mutta järkevämpi
  järjestys on maasto ensin, koska rakennukset tarvitsevat maastotyyppejä joita ei
  vielä ole (vuori, mäki, rannikko)

## Ei tehty, tarkoituksella — tarkista tiketeistä ennen kuin oletat mitään

- `BRDC-SCALE-001`:n alueperusteinen kysely — odottaa `BRDC-PERSIST-002`:ta
- `BRDC-CASTLE-001`:n `world.json`-vienti — odottaa `BRDC-SHARE-001`:tä
- Kaikki muut Vaihe 3 -tiketit (`TERRAIN-002`, `BUILD-001..003`, `TECH-001`,
  `MANA-001`, `SPELL-001`, `REVEAL-001`, `WONDER-001`, `CITY-001`, `EVENT-001`,
  `QUEST-001`, `ART-001`, `STATS-001`, `WIKI-001`, `TUTOR-001`, `ACHIEVE-001`,
  `ATLAS-001`, `ASCII-001`) — kirjoitettu auki, ei aloitettu koodina
- `BRDC-MOBILE-001` (ulkotesti) — ei voi ajaa koneelta, vaatii puhelimen ulkona

## Pieniä huomioita seuraavalle koneelle

- **Git-identiteetti ei ole asetettu** (ei globaalia `user.name`/`user.email`) —
  jokainen committi tuottaa Gitin oman auto-tunnistus-varoituksen. Ei haittaa toimintaa,
  mutta `git config --global user.name/user.email` poistaisi varoituksen
- **Node on v20.20.1**, `package.json` haluaa `>=22` — kaikki toimii silti (pelkkä
  varoitus jokaisessa pnpm-komennossa), ei korjattu koska ei liity mihinkään tehtyyn
- **`git push` vaatii interaktiivisen GitHub-kirjautumisen** tässä ympäristössä
  (`credential.helper = manager`) — ensimmäinen push voi jäädä odottamaan selainikkunaa
- Kaksi tiedostoa lähellä 400 rivin rajaa, syytä tarkistaa ennen seuraavaa lisäystä
  niihin: `apps/game/src/app/MapView.tsx` (365) ja `apps/game/src/features/map/MapCanvas.tsx` (322)
